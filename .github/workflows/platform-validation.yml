name: Platform validation

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: platform-validation-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            manager: both
            host_dry_run: true
          - os: ubuntu-latest
            manager: both
            host_dry_run: true
          - os: macos-latest
            manager: npm
            host_dry_run: false
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.23.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Prepare canonical Windows temp directory
        if: runner.os == 'Windows'
        shell: pwsh
        run: New-Item -ItemType Directory -Force ../afd-ci-temp | Out-Null
      - name: Full project checks
        if: runner.os == 'Windows'
        env:
          TEMP: ${{ github.workspace }}\..\afd-ci-temp
          TMP: ${{ github.workspace }}\..\afd-ci-temp
        run: pnpm check
      - name: Build release candidate
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          TEMP: ${{ github.workspace }}\..\afd-ci-temp
          TMP: ${{ github.workspace }}\..\afd-ci-temp
        run: |
          $version = (Get-Content package.json | ConvertFrom-Json).version
          pnpm release:check -- "v$version"
          ./scripts/release-audit.ps1
          pnpm release:build -- --skip-check
      - name: Portable project checks
        if: runner.os != 'Windows'
        run: pnpm lint && pnpm typecheck && pnpm build && pnpm docs:check
      - name: Build portable smoke tarball
        if: runner.os != 'Windows'
        run: node -e "require('fs').mkdirSync('release',{recursive:true})" && pnpm pack --pack-destination release
      - name: Smoke installed package outside the repository
        shell: pwsh
        run: node scripts/package-smoke.mjs --tarball release --manager "${{ matrix.manager }}" ${{ matrix.host_dry_run && '--host-dry-run' || '' }}
      - run: node scripts/afd-bootstrap.mjs --help
